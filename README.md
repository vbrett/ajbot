# ajbot

## Features

## migrate

## Configure Test environment in VScode

- enable pytest
- select `tests` as test folder
- Add following args (look for `pytest Args` in vscode settings)
  - --approvaltests-use-reporter=`'PythonNative'` or `'BeyondCompare'` or `'WinMerge'`
