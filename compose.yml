services:

  backend:
    image: ajbot:0.7.2b-arm7
    restart: unless-stopped
    pull_policy: never
    container_name: ajbot
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    volumes:
      - ".env:/.env"
