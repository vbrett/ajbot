services:

  backend:
    image: ajbot:0.2.3
    restart: unless-stopped
    pull_policy: never
    container_name: ajbot
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    volumes:
      - ".env:/.env"
